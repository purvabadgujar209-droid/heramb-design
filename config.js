// config.js — one setting you need to edit.
//
// After you deploy the "backend" folder (see backend/DEPLOY.md), you'll get a
// URL like "https://heramb-backend.onrender.com". Paste it below, replacing
// the example URL. Keep it WITHOUT a trailing slash.
//
// While testing on your own computer with the backend running locally,
// leave it as "http://localhost:4000".

window.HERAMB_API_BASE = "http://localhost:4000";
